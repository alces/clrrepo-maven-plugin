<project>
	<target name="purgetarget" if="${actualClear}">
		<echo>Purging ${repoPath}...</echo>
		<delete>
			<fileset dir="${repoPath}">
				<include name="**/*.jar"/>
				<include name="**/*.pom"/>
				<include name="**/*.sha1"/>
			</fileset>
		</delete>	
	</target>
</project>
