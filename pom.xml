<project>
	<modelVersion>4.0.0</modelVersion>
	<groupId>com.blogspot.cervus_alces</groupId>
	<artifactId>clrrepo-maven-plugin</artifactId>
	<packaging>maven-plugin</packaging>
	<version>1.0.0</version>
	<dependencies>
		<dependency>
			<groupId>org.apache.maven</groupId>
			<artifactId>maven-script-ant</artifactId>
			<version>2.2.1</version>
		</dependency>
	</dependencies>
	<!--
	Used by antrun plugin configuration (see below)
	<properties>
		<skip.repo.clean>true</skip.repo.clean>
	</properties>
	-->
	<build>
		<plugins>
			<plugin>
				<artifactId>maven-plugin-plugin</artifactId>
				<version>3.2</version>
				<dependencies>
					<dependency>
						<groupId>org.apache.maven.plugin-tools</groupId>
						<artifactId>maven-plugin-tools-ant</artifactId>
						<version>3.2</version>
					</dependency>
				</dependencies>
			</plugin>
			<!--
			// 3 ways to clear local repository
			// 1. clean plugin variant
			<plugin>
				<artifactId>maven-clean-plugin</artifactId>
				<version>2.1</version>
				<configuration>
					<filesets>
						<fileset>
							<directory>${settings.localRepository}</directory>
							<includes>
								<include>**/*.jar</include>
								<include>**/*.pom</include>
								<include>**/*.sha1</include>
							</includes>
						</fileset>
					</filesets>
				</configuration>
			</plugin>
			// 2. antrun variant
			<plugin>
				<artifactId>maven-antrun-plugin</artifactId>
				<version>1.7</version>
				<executions>
					<execution>
						<id>clear-repo</id>
						<phase>pre-clean</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<skip>${skip.repo.clean}</skip>
							<target>							
								<echo>Purging ${settings.localRepository}...</echo>
								<delete>
									<fileset dir="${settings.localRepository}">
										<include name="**/*.jar"/>
										<include name="**/*.pom"/>
										<include name="**/*.sha1"/>
									</fileset>
								</delete>
							</target>
						</configuration>
					</execution>
				</executions>
			</plugin>			
			// 3. own variant
			<plugin>
				<groupId>com.blogspot.cervus_alces</groupId>
				<artifactId>clrrepo-maven-plugin</artifactId>
				<executions>
					<execution>
						<id>clear-repo</id>
						<phase>pre-clean</phase>
						<goals>
							<goal>purge</goal>
						</goals>
					</execution>
				</executions>
			</plugin>			
			-->
		</plugins>
	</build>
	<distributionManagement>
	  <repository>
		<id>local_nexus</id>
		<name>Local nexus releases</name>
		<url>http://127.0.0.1:8080/content/repositories/releases</url>
	  </repository>
	  <snapshotRepository>
		<id>local_nexus</id>
		<name>Local nexus snapshots</name>
		<url>http://127.0.0.1:8080/content/repositories/snapshots</url>
	  </snapshotRepository>
	</distributionManagement>
</project>
